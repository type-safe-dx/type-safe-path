pre-commit:
  parallel: true
  commands:
    # lint:
    #   glob: '*.ts'
    #   run: pnpm rome format {staged_files}
    type:
      glob: '*.ts'
      run: pnpm tsc --noEmit
    format:
      # glob: '*.{yml,yaml,md,json,ts}'
      glob: '*.{ts}'
      run: pnpm rome format {staged_files}
